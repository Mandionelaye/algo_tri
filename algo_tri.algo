PROCEDURE permutation(VAR n, VAR m : INTEGER)
VAR
   p : INTEGER;
BEGIN
  p := n;
  n := m;
  m := p;
END

/* tri insertion  */

PROCEDURE tri_insertion(VAR tab : ARRAY_OF INTEGER)
VAR
   i,j,n: INTEGER;
BEGIN
   n := tab.length;
   FOR i FROM 0 TO n- 1 STEP 1 DO
      j:=i-1;
      WHILE (j>=o and tab[j]>=tab[j+1]) DO
        permutation(t[j],t[j+1]);
        j:=j-1;
      END_WHILE
   END_FOR
END
